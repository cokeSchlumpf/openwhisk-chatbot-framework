const _ = require('lodash');
const chai = require('chai');
const requireMock = require('mock-require');
const sinon = require('sinon');

const action = require('./index');

describe('channels-facebook-input', () => {
  it('is a stupid test', () => {
    action.main({
      payload: {
        
      },
      request: {
        body: {
          
        }
      }
    })
  });
});